apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting-system-dev-gitlab
  namespace: argocd
  labels:
    environment: dev
    source: gitlab
spec:
  project: default
  
  source:
    repoURL: https://gitlab.kiv.zcu.cz/zaverecne-prace/voting-system-gitops.git
    targetRevision: dev  
    path: charts/voting-system
    helm:
      valueFiles:
        - values.yaml
        - values-dev.yaml
  
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  
  syncPolicy:
    automated:
      prune: true       
      selfHeal: true    
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

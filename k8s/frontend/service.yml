apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: voting-system
  labels:
    app: frontend
spec:
  type: ClusterIP  # Доступ только через Ingress
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 3000
    name: http
